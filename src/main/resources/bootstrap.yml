spring: 
  profiles:
    active: development
  application:
    name: cibp-testbus-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
server:
  port: 9090
management:
  security:
    enabled: false
  